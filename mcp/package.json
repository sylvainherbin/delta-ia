{
  "name": "delta-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Serveur MCP distant, en lecture seule, sur les données publiques de Delta",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": { "test": "node --test test/mcp.test.js", "local": "node test/serveur-local.js" }
}
